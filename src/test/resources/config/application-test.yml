spring:
  cloud:
    bus:
      enabled: false
  sleuth:
    stream:
      enabled: false
hystrix:
  stream:
    queue:
      enabled: false
data:
  dir: src/main/resources
eureka:
  client:
    enabled: false
